# Commandes terminal pour MongoDB

Arrêter

```
sudo systemctl stop mongod
```

Démarrer

```
sudo systemctl start mongod
```

Vérifier le statut

```
sudo systemctl status mongod
```

Redémarrer après modifications

```
sudo systemctl restart mongod
```
